<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TechBlog!</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-neutral-200">
  <container class="flex flex-row h-24 justify-start items-center
  bg-gradient-to-r from-cyan-400 to-blue-500 drop-shadow-xl">
    <h1 class="px-8 text-3xl text-white font-bold">Tech Blog</h1>
    <container class="flex flex-row w-64 justify-around">
        {{#if logged_in}}
        <h1 class="font-bold text-white ml-5 mr-3" href="/profile"><a href="/profile">Profile</a></h1>
        <h1 class="font-bold text-white mr-3"><a href="#" id="logout">Logout</a></h1>
        <h1 class="text-white font-bold mr-3"><a href="/api/post">Post</a></h1>
        {{else}}
        <h1 class="font-bold text-white mr-3"><a href="/login">Login</a></h1>
        {{/if}}
      <h1 class="text-white font-bold mr-3"><a href="/">Home</a></h1>
    </container>
  </container>
  <div>
    <main class="container container-fluid m-auto">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>

    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="./js/logout.js"></script>
  {{/if}}
</body>

</html>