<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TechBlog!</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-neutral-200">
  <container class="flex flex-row h-24 justify-start items-center
  bg-gradient-to-r from-cyan-400 to-blue-500">
    <h1 class="px-8 text-3xl text-white font-bold">Tech Blog</h1>
    <container class="flex flex-row w-64 justify-around">
      {{#if logged_in}}
      <a href="/profile">profile</a>
      <button class="no-button" id="logout">logout</button>
      {{else}}
      <h1 class="font-bold text-white"><a href="#">Login</a></h1>
      {{/if}}
      <h1 class="text-white font-bold"><a href="#">Home</a></h1>
      <h1 class="text-white font-bold"><a href="#">Dashboard</a></h1>
    </container>
  </container>
  <div>
    <main class="container container-fluid m-auto">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>

    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>